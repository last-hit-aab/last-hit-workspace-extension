{
	"name": "last-hit-workspace-extension",
	"version": "0.9.8",
	"description": "workspace extension abstract implementation",
	"main": "dist/index.js",
	"scripts": {
		"compile": "gulp",
		"update:dev": "yarn add last-hit-types@dev",
		"deploy": "npm run compile && npm publish --access public",
		"deploy:dev": "npm run compile && npm publish --tag dev"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/last-hit-aab/last-hit-workspace-extension.git"
	},
	"bugs": {
		"url": "https://github.com/last-hit-aab/last-hit-workspace-extension/issues"
	},
	"homepage": "https://www.last-hit.com",
	"author": "last-hit-team",
	"license": "MIT",
	"private": false,
	"dependencies": {
		"@types/node": "^13.5.3",
		"decache": "^4.5.1",
		"last-hit-types": "^0.9.7",
		"typescript": "^3.7.3"
	},
	"devDependencies": {
		"gulp": "^4.0.2",
		"gulp-sourcemaps": "^2.6.5",
		"gulp-typescript": "^6.0.0-alpha.1"
	}
}
